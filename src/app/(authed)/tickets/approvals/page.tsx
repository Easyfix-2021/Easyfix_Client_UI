/*
 * Client Delay — temporarily hidden.
 * The full implementation is preserved in git history; restore it with
 * `git checkout <ref> -- src/app/(authed)/tickets/approvals/page.tsx`.
 */
import { ComingSoon } from '@/components/coming-soon';

export default function ClientDelayPage() {
  return <ComingSoon title="Client Delay" description="This section is temporarily unavailable." />;
}
