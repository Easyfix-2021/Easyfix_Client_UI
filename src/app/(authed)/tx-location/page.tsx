/*
 * Tx on Location — temporarily hidden.
 * The full implementation is preserved in git history; restore it with
 * `git checkout <ref> -- src/app/(authed)/tx-location/page.tsx`.
 */
import { ComingSoon } from '@/components/coming-soon';

export default function TxOnLocationPage() {
  return <ComingSoon title="Tx on Location" description="This section is temporarily unavailable." />;
}
