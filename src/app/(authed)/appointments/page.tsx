/*
 * Committed Appointments — temporarily hidden.
 * The full implementation is preserved in git history; restore it with
 * `git checkout <ref> -- src/app/(authed)/appointments/page.tsx`.
 */
import { ComingSoon } from '@/components/coming-soon';

export default function CommittedAppointmentsPage() {
  return <ComingSoon title="Committed Appointments" description="This section is temporarily unavailable." />;
}
